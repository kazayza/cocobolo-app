name: cocobolo_app
description: COCOBOLO CRM Application
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.4.0 <5.0.0'  # أحدث إصدارات Flutter

dependencies:
  flutter:
    sdk: flutter

  google_fonts: ^6.2.1
  flutter_animate: ^4.5.0
  animated_text_kit: ^4.2.2
  image_picker: ^1.1.2
  font_awesome_flutter: ^10.6.0
  url_launcher: ^6.2.1  # ✅ أضف ده
  intl: ^0.18.1  # ✅ أضف ده

  
  # Firebase - أحدث إصدارات (محدث)
  firebase_core: ^4.2.1
  firebase_messaging: ^16.0.4
  flutter_local_notifications: ^19.5.0
  timezone: ^0.10.1  # مطلوب لـ flutter_local_notifications

  http: ^1.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0  # محدث

flutter:
  uses-material-design: true

  assets:
    - assets/images/
  #  - assets/sounds/  # لو هتستخدم صوت تنبيه مخصص