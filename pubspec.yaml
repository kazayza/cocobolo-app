name: cocobolo_app
description: COCOBOLO CRM Application
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.4.0 <5.0.0'  # أحدث إصدارات Flutter

dependencies:
  flutter:
    sdk: flutter

  google_fonts: ^6.2.1
  flutter_animate: ^4.5.0
  animated_text_kit: ^4.2.2
  image_picker: ^1.1.2

  # Firebase - أحدث إصدارات شغالة 100% مع Flutter 3.22+
  firebase_core: ^3.6.0
  firebase_messaging: ^15.1.3
  flutter_local_notifications: ^17.2.3

  http: ^1.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/images/
  #  - assets/sounds/  # لو هتستخدم صوت تنبيه مخصص